{{- if .Values.extproc.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ default (printf "%s-gateway-ingress" (include "agent-sandbox-worker.fullname" .)) .Values.extproc.ingress.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "agent-sandbox-worker.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.extproc.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  ingressClassName: {{ .Values.extproc.ingress.className }}
  rules:
    - host: {{ .Values.extproc.ingress.host }}
      http:
        paths:
          - path: /agent-sandbox/api/native(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ include "agent-sandbox-worker.apiServiceName" . }}
                port:
                  number: 80
          - path: /agent-sandbox/api/data(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ include "agent-sandbox-worker.dataplaneServiceName" . }}
                port:
                  number: 80
          - path: /agent-sandbox/api/e2b(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ include "agent-sandbox-worker.e2bServiceName" . }}
                port:
                  number: 80
{{- end }}

{{- if .Values.crds.create }}
# DO NOT EDIT — auto-generated by hack/scripts/generate-helm.py
# Source: config/crd/bases/agents.navix.sh_sandboxenvs.yaml  (produced by `make manifests`)
# Run `make sync-crds-to-helm` to regenerate this file.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
    helm.sh/resource-policy: keep
  name: sandboxenvs.agents.navix.sh
spec:
  group: agents.navix.sh
  names:
    kind: SandboxEnv
    listKind: SandboxEnvList
    plural: sandboxenvs
    shortNames:
    - sbe
    singular: sandboxenv
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.mode
      name: Mode
      type: string
    - jsonPath: .status.localMemberCount
      name: Members
      type: integer
    - jsonPath: .status.pendingRequests
      name: Pending
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SandboxEnv is the Schema for the sandboxenvs API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object.

              Servers should convert recognized schemas to the latest internal value,
              and

              may reject unrecognized values.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents.

              Servers may infer this from the endpoint the client submits requests
              to.

              Cannot be updated.

              In CamelCase.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SandboxEnv
            properties:
              autoscaling:
                description: 'Autoscaling configures the Env-level autoscaler. When
                  nil or

                  Autoscaling.Enabled=false, member Pool replicas are managed manually.'
                properties:
                  enabled:
                    default: false
                    description: 'Enabled toggles the autoscaler on/off. When false,
                      member Pool replicas

                      are managed manually.'
                    type: boolean
                  groups:
                    description: 'Groups is the list of autoscaling groups. In Phase
                      1 there is exactly

                      one group named "default" populated from the source Pool''s
                      autoscaling.'
                    items:
                      description: 'EnvAutoscalingGroup is one Env-level autoscaling
                        unit, applied jointly to

                        every member referencing this group.'
                      properties:
                        maxReplicas:
                          description: 'MaxReplicas is the upper bound for the aggregate
                            (group) replica count.

                            Phase 1 (single member) treats this as the member''s effective
                            ceiling.'
                          format: int32
                          type: integer
                        minReplicas:
                          description: MinReplicas is the lower bound for the aggregate
                            (group) replica count.
                          format: int32
                          minimum: 0
                          type: integer
                        name:
                          description: Name matches EnvClusterMember.ScalingGroup.
                            Required.
                          type: string
                        scaleDownPolicy:
                          description: ScaleDownPolicy reuses the existing SandboxPool
                            scale-down semantics.
                          properties:
                            idleTimeoutSeconds:
                              default: 300
                              description: 'IdleTimeoutSeconds is the minimum duration
                                (in seconds) a Pod must remain

                                in Idle state before it becomes a candidate for scale-down.

                                Defaults to 300 (5 minutes).'
                              format: int32
                              minimum: 0
                              type: integer
                            protectionWindowSeconds:
                              default: 10
                              description: 'ProtectionWindowSeconds is the time window
                                after a Pod is marked for

                                scale-down (via the scale-down-protected annotation)
                                during which a new

                                Create Sandbox request can still claim it, cancelling
                                the scale-down intent.

                                Defaults to 10.'
                              format: int32
                              minimum: 0
                              type: integer
                            stabilizationSeconds:
                              default: 60
                              description: 'StabilizationSeconds is the minimum number
                                of seconds between two consecutive

                                scale-down events. Prevents thrashing when load fluctuates
                                around the threshold.

                                Defaults to 60.'
                              format: int32
                              minimum: 0
                              type: integer
                          type: object
                        scaleUpPolicy:
                          description: 'ScaleUpPolicy reuses the existing SandboxPool
                            scale-up semantics

                            (Conservative/Default/Aggressive modes, cooldown, idle-threshold).'
                          properties:
                            cooldownSeconds:
                              default: 30
                              description: 'CooldownSeconds is the minimum number
                                of seconds between two consecutive

                                scale-up events. Prevents scale-up storms when many
                                requests arrive simultaneously.

                                Defaults to 30.'
                              format: int32
                              minimum: 0
                              type: integer
                            idleThresholdSeconds:
                              default: 30
                              description: 'IdleThresholdSeconds triggers a proactive
                                scale-up when idleReplicas == 0

                                has persisted for this many seconds. Set to 0 to disable
                                proactive scale-up.

                                Defaults to 30.'
                              format: int32
                              minimum: 0
                              type: integer
                            mode:
                              allOf:
                              - enum:
                                - Conservative
                                - Default
                                - Aggressive
                              - enum:
                                - Conservative
                                - Default
                                - Aggressive
                              default: Default
                              description: "Mode controls how aggressively the pool\
                                \ grows on each scale-up decision.\n  - Conservative:\
                                \ +1 per decision\n  - Default:      +max(1, ceil(currentReplicas/2))\n\
                                \  - Aggressive:   scale to min(currentReplicas*2,\
                                \ maxReplicas)\nDefaults to Default."
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                type: object
              clusters:
                description: 'Clusters is the per-cluster member list. Each segment
                  is owned exclusively

                  by the Worker whose ClusterID matches; foreign segments are read-only
                  to

                  other Workers. Hub merges contributions from all Workers in Phase
                  2.'
                items:
                  description: EnvClusterSpec is the per-cluster portion of an Env
                    spec.
                  properties:
                    clusterID:
                      description: 'ClusterID identifies the cluster that owns this
                        segment. Each Worker

                        only mutates the segment matching its own ClusterID.'
                      type: string
                    members:
                      description: 'Members is the list of SandboxPool members contributed
                        by this cluster.

                        Phase 1 supports exactly one member per cluster.'
                      items:
                        description: EnvClusterMember describes one SandboxPool participating
                          in this Env.
                        properties:
                          inlineResources:
                            description: 'InlineResources is the Phase 1 migration
                              escape hatch: when the source

                              SandboxPool predates InstanceType labelling, the Pool''s
                              PodSpec resources

                              are copied here verbatim. New Envs created via the Dashboard
                              should leave

                              this empty and use InstanceType+Multiplier instead.'
                            properties:
                              claims:
                                description: 'Claims lists the names of resources,
                                  defined in spec.resourceClaims,

                                  that are used by this container.


                                  This field depends on the

                                  DynamicResourceAllocation feature gate.


                                  This field is immutable. It can only be set for
                                  containers.'
                                items:
                                  description: ResourceClaim references one entry
                                    in PodSpec.ResourceClaims.
                                  properties:
                                    name:
                                      description: 'Name must match the name of one
                                        entry in pod.spec.resourceClaims of

                                        the Pod where this field is used. It makes
                                        that resource available

                                        inside a container.'
                                      type: string
                                    request:
                                      description: 'Request is the name chosen for
                                        a request in the referenced claim.

                                        If empty, everything from the claim is made
                                        available, otherwise

                                        only the result of this request.'
                                      type: string
                                  required:
                                  - name
                                  type: object
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                              limits:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: 'Limits describes the maximum amount
                                  of compute resources allowed.

                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                                type: object
                              requests:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: 'Requests describes the minimum amount
                                  of compute resources required.

                                  If Requests is omitted for a container, it defaults
                                  to Limits if that is explicitly specified,

                                  otherwise to an implementation-defined value. Requests
                                  cannot exceed Limits.

                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                                type: object
                            type: object
                          instanceType:
                            description: 'InstanceType references an entry in the
                              cluster-wide InstanceType catalog.

                              Mutually informative with InlineResources: if both are
                              set, InstanceType

                              wins and InlineResources serves as a transitional record
                              for migration.'
                            type: string
                          maxReplicas:
                            description: "MaxReplicas is the upper bound on this member's\
                              \ spec.replicas. Reserved\nfor Phase 2 multi-member\
                              \ routing \u2014 enforced by the Env autoscaler when\n\
                              distributing scale-up delta across members. Phase 1\
                              \ ignores it."
                            format: int32
                            minimum: 0
                            type: integer
                          multiplier:
                            description: Multiplier scales InstanceType's resources.
                              Required when InstanceType is set.
                            format: int32
                            minimum: 0
                            type: integer
                          name:
                            description: 'Name is the SandboxPool''s metadata.name
                              within the Env''s namespace.

                              Acts as the list map key for Members.'
                            type: string
                          priority:
                            description: 'Priority controls routing order: lower priority
                              is preferred. Reserved

                              for Phase 2; Phase 1 ignores it.'
                            format: int32
                            type: integer
                          scaleDownPriority:
                            description: 'ScaleDownPriority controls scale-down order:
                              lower values shrink first.

                              Reserved for Phase 2; Phase 1 ignores it.'
                            format: int32
                            type: integer
                          scaleUpPriority:
                            description: 'ScaleUpPriority controls scale-up order
                              within a scalingGroup: lower

                              values are scaled up first. Same-value tiebreak: (clusterID,
                              name)

                              lexicographic. Reserved for Phase 2; Phase 1 ignores
                              it.'
                            format: int32
                            type: integer
                          scalingGroup:
                            default: default
                            description: "ScalingGroup names the autoscaling group\
                              \ this member belongs to. Members\nin the same group\
                              \ must share the same effective resources (= InstanceType\n\
                              \xD7 Multiplier or identical InlineResources). Empty\
                              \ means the member is\nexcluded from autoscaling."
                            type: string
                        required:
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                  required:
                  - clusterID
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - clusterID
                x-kubernetes-list-type: map
              defaults:
                description: 'Defaults supplies the InstanceType and multiplier used
                  when a Sandbox.create

                  request does not specify them explicitly. Strongly recommended.'
                properties:
                  instanceType:
                    description: "InstanceType references an entry in the cluster-wide\
                      \ InstanceType catalog.\nMay be empty when the Env was migrated\
                      \ from a legacy SandboxPool that did\nnot carry an InstanceType\
                      \ label \u2014 in that case members use InlineResources."
                    type: string
                  multiplier:
                    description: 'Multiplier scales the InstanceType''s base resources.
                      Must fall within the

                      InstanceType''s declared [min, max] range; validated by the
                      Env Controller.'
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              mode:
                default: WarmPool
                description: 'Mode selects between WarmPool (predefined member Pools)
                  and OnDemandJob

                  (per-request SandboxJob).'
                enum:
                - WarmPool
                - OnDemandJob
                type: string
              templateRef:
                description: 'TemplateRef binds this Env to exactly one SandboxTemplate
                  (runtime). All

                  member Pools must reference the same Template.'
                properties:
                  name:
                    description: Name of the SandboxTemplate (cluster-scoped).
                    type: string
                  version:
                    description: 'Version optionally pins the Env to a specific Template
                      version. When

                      empty, the Template''s current spec.version is observed and
                      recorded in

                      status.'
                    type: string
                required:
                - name
                type: object
            required:
            - mode
            - templateRef
            type: object
          status:
            description: status defines the observed state of SandboxEnv
            properties:
              clusters:
                description: 'Clusters carries the per-cluster observed state. Worker
                  writes only the

                  segment with IsLocal=true; other segments are populated by Hub Sync.'
                items:
                  description: EnvClusterStatus is the per-cluster observed state.
                  properties:
                    clusterID:
                      description: ClusterID matches the spec's ClusterID for the
                        same segment.
                      type: string
                    idleZeroSince:
                      description: 'IdleZeroSince records when the aggregated idle
                        count for this cluster

                        first dropped to zero in the current continuous-zero window.
                        Cleared

                        when idle > 0.'
                      format: date-time
                      type: string
                    isLocal:
                      description: 'IsLocal is true on the Worker that owns this cluster''s
                        Pools. Used to

                        gate writes: only IsLocal=true segments are mutated by the
                        local Env

                        Reconciler.'
                      type: boolean
                    lastScaleDownTime:
                      description: LastScaleDownTime records the latest cluster-local
                        scale-down event.
                      format: date-time
                      type: string
                    lastScaleUpTime:
                      description: 'LastScaleUpTime records the latest cluster-local
                        scale-up event. Used

                        by the Env autoscaler for cooldown enforcement.'
                      format: date-time
                      type: string
                    lastSnapshotTime:
                      description: 'LastSnapshotTime records when this segment was
                        last updated. For

                        IsLocal=true: write time by the local Reconciler. For IsLocal=false:

                        arrival time of the Hub Sync push.'
                      format: date-time
                      type: string
                    observedMembers:
                      description: 'ObservedMembers reports per-member runtime state
                        (idle/running/desired,

                        effective resources, member state).'
                      items:
                        description: EnvObservedMember reports per-member runtime
                          state.
                        properties:
                          currentReplicas:
                            description: CurrentReplicas is the value last observed
                              on the Pool spec.
                            format: int32
                            type: integer
                          desiredReplicas:
                            description: 'DesiredReplicas is the most recent value
                              the Env autoscaler patched onto

                              the member Pool''s spec.replicas.'
                            format: int32
                            type: integer
                          effectiveResources:
                            description: "EffectiveResources is the resolved resource\
                              \ request/limit per Pod\n(= InstanceType.resources \xD7\
                              \ Multiplier, or InlineResources verbatim)."
                            properties:
                              claims:
                                description: 'Claims lists the names of resources,
                                  defined in spec.resourceClaims,

                                  that are used by this container.


                                  This field depends on the

                                  DynamicResourceAllocation feature gate.


                                  This field is immutable. It can only be set for
                                  containers.'
                                items:
                                  description: ResourceClaim references one entry
                                    in PodSpec.ResourceClaims.
                                  properties:
                                    name:
                                      description: 'Name must match the name of one
                                        entry in pod.spec.resourceClaims of

                                        the Pod where this field is used. It makes
                                        that resource available

                                        inside a container.'
                                      type: string
                                    request:
                                      description: 'Request is the name chosen for
                                        a request in the referenced claim.

                                        If empty, everything from the claim is made
                                        available, otherwise

                                        only the result of this request.'
                                      type: string
                                  required:
                                  - name
                                  type: object
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                              limits:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: 'Limits describes the maximum amount
                                  of compute resources allowed.

                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                                type: object
                              requests:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: 'Requests describes the minimum amount
                                  of compute resources required.

                                  If Requests is omitted for a container, it defaults
                                  to Limits if that is explicitly specified,

                                  otherwise to an implementation-defined value. Requests
                                  cannot exceed Limits.

                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                                type: object
                            type: object
                          idleCount:
                            description: 'IdleCount, RunningCount are mirrored from
                              SandboxPool.status to surface

                              a single Env-level view to the Dashboard.'
                            format: int32
                            type: integer
                          instanceType:
                            description: InstanceType / Multiplier are echoed from
                              spec for convenience.
                            type: string
                          multiplier:
                            format: int32
                            type: integer
                          name:
                            description: Name matches the spec member's Name and is
                              the list map key.
                            type: string
                          runningCount:
                            format: int32
                            type: integer
                          state:
                            description: State summarises whether the member can currently
                              serve requests.
                            enum:
                            - Active
                            - Saturated
                            - Missing
                            - Inconsistent
                            type: string
                        required:
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                  required:
                  - clusterID
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - clusterID
                x-kubernetes-list-type: map
              conditions:
                description: "Conditions surfaces high-level Env health signals (Ready,\n\
                  TemplateConsistent, AutoscalingActive, \u2026)."
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: 'lastTransitionTime is the last time the condition
                        transitioned from one status to another.

                        This should be when the underlying condition changed.  If
                        that is not known, then using the time when the API field
                        changed is acceptable.'
                      format: date-time
                      type: string
                    message:
                      description: 'message is a human readable message indicating
                        details about the transition.

                        This may be an empty string.'
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: 'observedGeneration represents the .metadata.generation
                        that the condition was set based upon.

                        For instance, if .metadata.generation is currently 12, but
                        the .status.conditions[x].observedGeneration is 9, the condition
                        is out of date

                        with respect to the current state of the instance.'
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: 'reason contains a programmatic identifier indicating
                        the reason for the condition''s last transition.

                        Producers of specific condition types may define expected
                        values and meanings for this field,

                        and whether the values are considered a guaranteed API.

                        The value should be a CamelCase string.

                        This field may not be empty.'
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - 'True'
                      - 'False'
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              localMemberCount:
                description: 'LocalMemberCount caches the length of the local cluster''s
                  Members for

                  use in printer columns (which can''t easily evaluate nested arrays).'
                format: int32
                type: integer
              pendingRequests:
                description: 'PendingRequests is the number of Sandbox.create requests
                  held by

                  EnvScheduler when no member could accept them (typically because
                  all

                  members hit Saturated). Drives the EnvScaleUpPending signal.'
                format: int32
                type: integer
              scalingGroups:
                description: 'ScalingGroups aggregates idle/running counts per scalingGroup
                  across all

                  members (across clusters when remote segments are populated by Sync).'
                items:
                  description: 'EnvScalingGroupStatus aggregates a scalingGroup''s
                    runtime state across all

                    members.'
                  properties:
                    name:
                      description: Name matches the autoscaling group's Name and is
                        the list map key.
                      type: string
                    totalDesired:
                      format: int32
                      type: integer
                    totalIdle:
                      description: TotalIdle / TotalRunning / TotalDesired aggregate
                        across members.
                      format: int32
                      type: integer
                    totalRunning:
                      format: int32
                      type: integer
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}

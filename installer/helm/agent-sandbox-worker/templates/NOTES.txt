Agent Sandbox has been deployed to namespace {{ .Release.Namespace }}.

CONTROLLER:
  API:       http://{{ include "agent-sandbox-worker.fullname" . }}-control-plane.{{ .Release.Namespace }}.svc.cluster.local/
  E2B API:   http://{{ include "agent-sandbox-worker.fullname" . }}-e2b-api.{{ .Release.Namespace }}.svc.cluster.local/
  Metrics:   http://{{ include "agent-sandbox-worker.fullname" . }}-controller-manager-metrics-service.{{ .Release.Namespace }}.svc.cluster.local:8082/metrics

DATA PLANE (ExtProc + Envoy):
  Gateway:   http://{{ include "agent-sandbox-worker.fullname" . }}-data-plane.{{ .Release.Namespace }}.svc.cluster.local/

{{- if .Values.extproc.ingress.enabled }}

GATEWAY INGRESS:
  https://{{ .Values.extproc.ingress.host }}/agentbox/api/native  → controller API
  https://{{ .Values.extproc.ingress.host }}/agentbox/api/data    → data-plane
  https://{{ .Values.extproc.ingress.host }}/agentbox/api/e2b     → E2B-compatible API
{{- end }}

NEXT STEPS:
  1. Create a SandboxTemplate:
       kubectl apply -f https://raw.githubusercontent.com/scitix/agent-sandbox/main/config/samples/envd_sandboxtemplate.yaml

  2. Create a SandboxPool:
       kubectl apply -f https://raw.githubusercontent.com/scitix/agent-sandbox/main/config/samples/agents_v1alpha1_sandboxpool.yaml

  3. Check pool status:
       kubectl get sandboxpool -A
